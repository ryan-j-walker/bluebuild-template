---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: silverblue

description: Universal Blue's silverblue-main with my tweaks.

base-image: ghcr.io/ublue-os/silverblue-main
image-version: latest

modules:
  - type: files
    files:
      - source: system/shared
        destination: /

  - from-file: common-recipes/common-fonts.yml
  - from-file: common-recipes/common-packages.yml
  - from-file: common-recipes/common-homebrew.yml
  - from-file: silverblue/gnome-extensions.yml
  - from-file: common-recipes/common-flatpaks.yml
  - from-file: common-recipes/common-systemd.yml
  
  - type: script
    scripts:
      - silverblue/theming.sh
      - common-scripts/common-cleanup.sh

  - type: signing # this sets up the proper policy & signing files for signed images to work fully

